<nav class="navbar navbar-expand-lg bg-white border-bottom shadow-sm">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center gap-2" href="/">
            <i class="bi bi-cpu-fill text-primary fs-4"></i>
            <span class="fw-bold text-dark">CoreApp <small class="fw-normal text-muted">Enterprise</small></span>
        </a>
    
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#topNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="topNavbar">
            <ul class="navbar-nav ms-auto align-items-lg-center gap-2">
                <li class="nav-item">
                    <a class="nav-link {{#if activeHome}}active fw-semibold text-primary{{/if}}" href="/"><i class="bi bi-house-door me-1"></i> Inicio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if activeStatus}}active fw-semibold text-primary{{/if}}" href="/status"><i class="bi bi-activity me-1"></i> Estado API</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle {{#if activeUsers}}active fw-semibold text-primary{{/if}}" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-people me-1"></i> Gestión de Usuarios
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end shadow-sm">
                        <li><a class="dropdown-item" href="/users"><i class="bi bi-list-task me-2"></i> Listar Usuarios</a></li>
                        <li><a class="dropdown-item" href="/users/add"><i class="bi bi-person-plus me-2"></i> Registrar Nuevo</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>